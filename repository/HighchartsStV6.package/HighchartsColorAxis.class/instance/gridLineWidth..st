accessing
gridLineWidth: aNumber

	"The width of the grid lines extending from the axis across the
gradient of a scalar color axis.
Defaults to 1."

	configurationOptions at: 'gridLineWidth' put: aNumber