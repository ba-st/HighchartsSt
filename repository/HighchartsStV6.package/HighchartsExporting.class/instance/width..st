accessing
width: aNumber

	"The pixel width of charts exported to PNG or JPG. As of Highcharts
3.0, the default pixel width is a function of the <a href='#chart.
width'>chart.width or <a href='#exporting.sourceWidth'>exporting.sourceWidth and the
<a href='#exporting.scale'>exporting.scale.
Defaults to undefined."

	configurationOptions at: 'width' put: aNumber