accessing
sourceWidth: aNumber

	"The width of the original chart when exported, unless an explicit
<a href='#chart.width'>chart.width is set. The width exported raster image
is then multiplied by <a href='#exporting.scale'>scale.
"

	configurationOptions at: 'sourceWidth' put: aNumber