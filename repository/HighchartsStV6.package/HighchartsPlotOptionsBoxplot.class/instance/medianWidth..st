accessing
medianWidth: aNumber

	"The pixel width of the median line. If null, the <a href='#plotOptions.
boxplot.lineWidth'>lineWidth is used.
Defaults to 2."

	configurationOptions at: 'medianWidth' put: aNumber