as yet unclassified
inputEnabled: aBoolean

	"Enable or disable the date input boxes. Defaults to enabled when there is enough space, disabled if not (typically mobile). "

	configurationOptions at: 'inputEnabled' put: aBoolean